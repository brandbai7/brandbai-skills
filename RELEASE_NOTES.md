# BrandBAI TikTok Download 0.2.1

这是 TikTok 品牌业务场景路由升级，不扩大平台访问权限，也不把下载 Skill 改成分析 Skill。

## 新能力

- 增加 `market-scan`、`influence-shortlist`、`creative-benchmark`、`campaign-reception` 和 `shop-affiliate-evidence` 五个业务预设；
- 先冻结市场、语言、来源界面、观察时区、授权模式和下游用途，再执行主页、搜索或单作品采集；
- 明确区分公开 TikTok 内容、Creative Center／TikTok One／Ads Manager／Seller Center 官方数据和后续分析结论；
- 增加跨市场任务字段、商业披露与商品锚点的可见状态合同；
- 把公开下载包正式路由到影响力对象分析、内容诊断、用户语义、商品匹配和增长规划。

## 边界

- 当前代码能力仍以公开页面下载、原文保留、本机双语、Excel、ZIP、断点和完整性为主；
- TikTok One 受众及项目报告、Ads Manager 投放数据、Seller Center 订单／GMV／佣金只接受品牌授权导出；
- 多时点官宣复盘当前由用户在指定时点主动重复运行，不承诺后台自动监控；
- 不从公开播放、点赞或评论推算受众、广告增量、销量或达人匹配。

---

# BrandBAI TikTok Download 0.2.0

这是与 BrandBAI 下载助手 v0.9.0 对齐的 TikTok 多语言证据升级。

## 新能力

- 单作品发布文案可由用户主动生成中英双语 Excel；达人主页和搜索页也可处理当前可见作品文案；
- 已采集一级评论可生成中英双语 Excel，并保留原文、识别语言、置信度、中文、English、状态、引擎和生成时间；
- 中文原文只补英文，英文原文只补中文，其他语言分别补中文和英文；纯表情、链接、数字和账号标识不强行翻译；
- 免费路线优先使用支持该能力的 Chrome 桌面版本机模型，不上传文本、不消耗云端字符额度，并缓存相同原文。

## 边界

- 本机翻译需要用户主动点击，首次使用可能下载语言模型；浏览器不支持或语言对失败时不静默调用云端计费；
- Python 采集脚本仍以原文采集和资料整理为主；本机双语功能由 BrandBAI 下载助手 v0.9.0 提供；
- AI 语音转文案与翻译、OCR、云端回退和大批量加速仍未上线；
- 译文只用于跨语言检索与初步理解，不替代发布者或评论者原话。

---

# BrandBAI Product Value 0.1.14

本次修订聚焦客户语义、FABE 内生任务优势和跨字段一致性：

- 结构化交付 schema 升级至 `0.1.7`，当前执行主轴必须显式绑定可调用价值；
- 清理普通版内部术语与隐藏 ID 后的残句；
- 拦截活动状态、优惠叠加和 blocked/deferred 执行轴的跨字段矛盾；
- 区分市场竞争优势与内生任务优势，避免因缺少竞品资料机械清空全部 FABE Advantage；
- 扩展无来源产品替代对象的同义表达，并以逻辑 `updated_at` 校验最终收口时间。

---

# BrandBAI Product Value 0.1.13

本次修订聚焦 P0 证据门槛、归档来源隔离和普通版一致性：

- 结构化交付 schema 升级至 `0.1.6`；
- `SC0/SC1` 且没有竞品或行业对照时，P0 只能保留为优先验证假设；
- 拦截“相较”及没有比较引导词的无来源产品替代对象，并阻止 A 层暂不成立后在其他字段继续比较；
- ZIP、RAR、7z、tar 等归档文件不得被登记为已读取页面或事实来源；
- 普通版必须由当前结构化账本确定性生成，拦截手工改稿、内部字段删减残片和语义矛盾残句。

---

# BrandBAI Product Value 0.1.12

本次修订聚焦客户文本完整性和 FABE Advantage 真实性：

- 结构化账本与普通版同步拦截空括号和缺少结论对象的分析残句；
- 禁止“本品（基于页面内对比信息）”等占位式 Advantage；
- 资料不足时允许明确保留“A层暂不成立”，并强制标为待验证；
- 增加真实失败形态的纯虚构离线回归，防止只修客户稿、不修上游账本。

---

# BrandBAI Product Value 0.1.8

本次修订聚焦真实审计时间、商品身份、动态权益和分析推导边界：

- 拦截固定间隔批量生成、重复、未来或晚于账本写入时间的图片核验与原文复核记录；
- 标题与包装、规格表或商品信息区冲突时，不再把标题片段继续写成当前 SKU；
- 活动年份由采集时间补全时必须披露依据，原文含具体时刻时必须保留日期、时刻和时区；
- 扩大无依据产品替代对象拦截，并禁止把体验描述自行升级成原文没有的食用限制；
- 增加对应离线回归测试，并用上一版真实交付反向验证新门槛。

---

# BrandBAI Product Value 0.1.7

本次修订聚焦真实审计、FABE边界和普通版候选完整性：

- 校验第二遍逆序复核的真实时间顺序，阻止脚本批量回填；
- 强制识别禁止食用、请勿食用、不宜食用和遵医嘱等关键警示；
- 拦截无依据的替代性比较、二氧化硫残留风险扩大、使用方式缩窄和优惠叠加；
- 普通版展示全部P0候选及未入选原因，并清理更多内部ID和英文技术状态；
- 正式交付根目录拒绝修正脚本与可执行文件。

---

# BrandBAI Value Expression 0.1.1

这是面向 SkillHub 等宿主的元数据兼容性修订版。将 `description` 从 YAML 折叠块改为语义不变的单行文本，避免只提取到 `>-`；同步 Skill 运行版本与独立安装包。能力、输入输出合同、证据边界和商品价值继承规则不变。

---

# BrandBAI Product Value 0.1.1

这是面向 SkillHub 等宿主的元数据兼容性修订版。将 `description` 从 YAML 折叠块改为语义不变的单行文本，避免只提取到 `>-`；同步 Skill 运行版本与独立安装包。能力、FABE、P0/P1/P2、证据边界及资料缺口合同不变。

---

# BrandBAI Value Expression 0.1.0

这是 BrandBAI 卖点呈现的首个公开原型版本。它只读取当前有效的商品价值底座，把已经确认的 P0/P1/P2 翻译成用户看得见、听得懂、感受得到的原子呈现，不重新选择价值，不直接生成完整脚本。

## 能力

- 继承上游商品、SKU、事实、FABE 结果、P0/P1/P2、已有页面表达和证据边界；
- 对每个可沟通价值逐项扫描数字化、感官化、差异化、情境化、证据化和人格化六条路径；
- 每个价值固定选择 1 条主路径与 1—2 条辅助路径，未选路径仍保留本轮不优先或不适用理由；
- 完整扫描十二类感知槽位，适用则建立 VIS，不适用则明确记录理由；
- 每个 VIS 只承担一个主价值和一个决策任务，并完整填写画面、动作、声音、字幕、道具五轨；
- 同步判断场景人物状态、特效／BGM、商品包装和商品页承接；
- 输出普通版 `01_卖点可视化呈现.md`、`02_资料说明与验证计划.md` 与可回溯结构化底稿；
- 提供 Dry Run、拒绝覆盖、上游状态校验、确定性报告生成、完整性校验和纯虚构离线测试。

## 边界

- 不重新选择或修改商品价值底座中的核心价值与价值分层；
- 不把包装漂亮、已有画面多或容易拍写成核心价值；
- 不把公开页面已有表达自动标为已验证资产；
- 没有业务目标、人群、账号／载体和交易承接时，不生成完整内容方向、钩子、脚本、达人原话或动态 CTA；
- 真实执行和数据对位前，新增呈现只能标为建议、候选或待验证。

## 授权

本版本采用 PolyForm Noncommercial License 1.0.0。企业内部使用、客户交付、收费服务、插件、SaaS、数据服务及其他预期商业用途，需要通过 **brandlaobai@163.com** 取得 BrandBAI 书面商业授权。

---

# BrandBAI Product Value 0.1.0

这是 BrandBAI 商品价值底座的首个公开原型版本，严格停止在商品事实、商品价值和证据边界，不包含卖点呈现或达人匹配。

## 能力

- 接收商品页、手卡、包装、说明、参数、SKU、检测证据、品牌战略、用户反馈或混合资料；
- 分开管理商品事实、第三方证据、品牌战略、动态权益、用户语言、已有表达和分析推导；
- 将与当前商品和 SKU 明确对应的公开详情页作为有效商品证据；第三方材料负责增强证据等级，不作为所有商品价值的准入门槛；
- 恢复严格的 FABE 中间分析层，每个非暂缓价值保留 Feature、Advantage、Benefit、Evidence、参照系、用户语言与边界；
- 建立商品身份、识别锚、P0 候选、P0/P1/P2、证据引用、表达边界和资料缺口；
- 区分 `complete`、`partial`、`insufficient`、`stale`，并控制下游 `ready`、`conditional`、`blocked`、`stale`；
- 生成 `01_商品价值底座.md`、`02_资料说明与缺口.md` 和可供后续 Skill 读取的结构化底稿；
- 普通版增加 FABE 价值证据链、核心价值分层理由和条件式下游应用地图，并隐藏内部资产 ID；
- 提供 Dry Run、拒绝覆盖、确定性报告生成、证据引用校验和纯虚构离线测试。

## 边界

- 不把页面信息自动写成第三方独立验证；
- 不把品牌战略、评论频次或分析推导写成用户已认可事实；
- 不生成 VIS、画面动作、脚本、Brief、达人匹配或商业归因；
- 后续 `brandbai-value-expression` 和匹配 Skill 只能继承当前有效的事实、价值、P0 状态及限制。

## 授权

本版本采用 PolyForm Noncommercial License 1.0.0。企业内部使用、客户交付、收费服务、插件、SaaS、数据服务及其他预期商业用途，需要通过 **brandlaobai@163.com** 取得 BrandBAI 书面商业授权。

---

# BrandBAI Douyin Download 0.3.0

这是与 BrandBAI Chrome 插件 v0.3.0 对齐的作品选择、搜索结果与交付打包升级。

## 新能力

- 继续支持达人主页“全部当前可见置顶＋最近 N 条非置顶作品”；
- 支持读取 BrandBAI Chrome 插件导出的 `作品清单.xlsx`，按用户在达人主页或搜索结果中选中的作品继续下载；
- 支持 `brandbai.douyin.selection/v1` JSON 选择合同，供其他模型和未来插件版本交接；
- 支持重复 `--video` 下载一个或多个明确作品，包括带 `modal_id` 的作品链接；
- 支持直接观察达人或搜索结果页，并通过 `--selected-id` 或 `--limit` 固定当前范围；
- 搜索页网络数据未被观察到时，可从当前可见卡片的公开作品 ID 继续进入正常作品页补齐元数据；
- `--assets` 可选择主视频／图文、封面、可用原声和发布文案；`--assets none` 支持只取作品数据；
- `all --skip-comments` 支持只生成作品数据与素材交付；
- `--zip` 可在交付目录同级生成 ZIP64 兼容压缩包；
- 发布文案保存为 UTF-8 文本，并明确不等同于口播转写。

## 数据与完整性

- 插件 Excel、选择 JSON、明确 URL 和页面观察最终统一为同一 `works.json` 合同；
- 搜索页保留来源页面类型、搜索词、来源排序、选择原因和选择顺序；
- 找不到的指定作品 ID 与素材下载失败分别记录，不能静默缩小范围；
- 选择文件未带媒体地址且正常作品页也未返回可用元数据时标记 `partial_metadata_unavailable`；
- 元数据模式不会创建空素材目录；未选择素材记录为 `not_requested`；
- ZIP 不包含 Chrome 登录资料夹、长任务目录或 QA 预览。

## 仍保持的稳定边界

- 一级评论仍需收到明确分页终止信号才能标记 `complete_source_visible`；
- 二级回复仍为实验能力，不承诺完整；
- 搜索结果只表示本次页面观察或用户导出的选择快照，不代表平台全部搜索结果；
- 下载阶段不自动生成语义标签、账号结论、商品匹配或商业归因。

## 验证

- 作品选择、插件 Excel、选择 JSON、modal_id、素材选项、搜索卡片回退和 ZIP 均使用合成数据测试；
- 原作品采集、评论采集、断点续跑、长任务和 Excel 回归测试继续保留；
- 自动化测试不打开抖音、不启动 Chrome、不产生付费请求；发布前已完成达人主页“最近 1 条、仅数据”真实验证；
- 已在登录 Chrome 中确认搜索结果卡片可见，并完成“搜索页选择合同 → 作品详情补齐 → 封面下载 → ZIP”真实端到端验证。独立采集浏览器直接打开搜索页时仍以该浏览器已经登录且结果可见为前提，默认优先使用插件导出的选择文件交接。

## 安装包

下载 `brandbai-douyin-download.zip`。压缩包根目录直接包含 `SKILL.md`，适合支持自定义 Agent Skills 的宿主安装。可使用同名 `.sha256` 文件核验完整性。

## 授权与边界

本版本采用 PolyForm Noncommercial License 1.0.0。企业内部使用、客户交付、收费服务、插件、SaaS、数据服务及其他预期商业用途，需要通过 **brandlaobai@163.com** 取得 BrandBAI 书面商业授权。

安装本 Skill 不等于获得抖音或第三方数据授权。使用者仍需遵守适用法律、平台规则、账号权限和个人信息保护要求。
